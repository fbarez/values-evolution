# Copy to .env and fill in — never commit .env
GESIS_USERNAME=
GESIS_PASSWORD=
ESS_API_KEY=
HF_TOKEN=
WB_API_KEY=       # World Bank (optional — most endpoints are public)
VDEM_API_KEY=     # V-Dem (optional — bulk download via UI)
